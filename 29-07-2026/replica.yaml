kind: ReplicaSet
apiVersion: apps/v1
metadata:
  name: httpd-pod
  namespace: httpd

spec:
  replicas: 5

  selector:
    matchLabels:
      app: httpd

  template:
    metadata:
       labels:
         app: httpd

    spec:
       containers:
       - name: httpd-pods
         image: httpd:latest
         ports:
         - containerPort: 80
                         

