kind: Pod
apiVersion: v1
metadata:
  name: nginx-pod
  namespace: nginx
spec:
  initContainers:
  - name: init-container
    image: busybox:latest
    command:
      - sh
      - -c
      - sleep 30
  containers:
  - name: busybox
    image: busybox:latest
    command:
      - sh
      - -c 
      - echo "hello sid"; sleep infinity
