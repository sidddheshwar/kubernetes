apiVersion: v1
kind: Pod
metadata:
  name: nginx-volume
  namespace: configmap-demo
spec:
  containers:
  - image: nginx
    name: nginx-volume-pod
    volumeMounts:
    - name: web-volume
      mountPath: /usr/share/nginx/html

  volumes:
  - name: web-volume
    configMap:
      name: web-config
